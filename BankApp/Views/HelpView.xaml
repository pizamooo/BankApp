<UserControl x:Class="BankApp.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BankApp.ViewModels"
             Background="#F4F6F9">

	<UserControl.DataContext>
		<vm:HelpViewModel/>
	</UserControl.DataContext>

	<ScrollViewer VerticalScrollBarVisibility="Auto">

		<StackPanel Margin="25">
			<TextBlock Text="Центр помощи"
                       FontSize="32"
                       FontWeight="Bold"
                       Margin="0 0 0 5"/>
			<TextBlock Text="Частые вопросы и руководство"
                       Foreground="Gray"
                       Margin="0 0 0 25"/>
			<ItemsControl ItemsSource="{Binding Faqs}">
				<ItemsControl.ItemTemplate>
					<DataTemplate>
						<Border Background="White"
                                CornerRadius="16"
                                Padding="18"
                                Margin="0 0 0 15">
							<StackPanel>
								<Button Background="Transparent"
                                        BorderThickness="0"
										Cursor="Hand"
                                        HorizontalContentAlignment="Stretch"
                                        Padding="0">
									<Button.Style>
										<Style TargetType="Button">
											<EventSetter Event="Click"
                                                         Handler="ToggleFaq"/>
										</Style>
									</Button.Style>
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition/>
											<ColumnDefinition Width="Auto"/>
										</Grid.ColumnDefinitions>
										<TextBlock Text="{Binding Question}"
                                                   FontWeight="SemiBold"
                                                   FontSize="16"/>
										<TextBlock Grid.Column="1"
                                                   Text="⌄"
                                                   FontSize="18"
                                                   Foreground="Gray"/>
									</Grid>
								</Button>
								<TextBlock Text="{Binding Answer}"
                                           Margin="0 15 0 0"
                                           Foreground="#475569"
                                           TextWrapping="Wrap">
									<TextBlock.Style>
										<Style TargetType="TextBlock">
											<Setter Property="Visibility"
                                                    Value="Collapsed"/>
											<Style.Triggers>
												<DataTrigger Binding="{Binding IsOpen}"
                                                             Value="True">
													<Setter Property="Visibility"
                                                            Value="Visible"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>
							</StackPanel>
						</Border>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>
			<Button Content="← Назад"
        Width="140"
        Height="42"
        FontWeight="Bold"
        Background="#2563EB"
        Foreground="White"
        BorderThickness="0"
        Cursor="Hand"
        Command="{Binding GoBackCommand}"/>
		</StackPanel>
	</ScrollViewer>

</UserControl>